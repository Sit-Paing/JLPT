import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sitpaing.aijlpt',
  appName: '藍 Ai',
  webDir: 'www',
  backgroundColor: '#1E2F4D',
  android: {
    allowMixedContent: true
  }
};

export default config;
